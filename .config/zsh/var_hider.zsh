#!/usr/bin/env zsh
#
# Hides a bunch of variables in typeset
typeset -H prompt
typeset -H _ZSH_AUTOSUGGEST_BIND_COUNTS
typeset -H __FAST_HIGHLIGHT_TOKEN_TYPES
typeset -H PROMPT
typeset -H PROMPT2
typeset -H PROMPT3
typeset -H PROMPT4
typeset -H PS1
typeset -H PS2
typeset -H PS3
typeset -H PS4
typeset -H RPROMPT
