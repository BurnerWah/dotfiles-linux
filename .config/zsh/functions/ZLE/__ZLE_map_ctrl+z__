#autoload
setopt LOCAL_OPTIONS
# setopt NO_MONITOR
if [[ $(jobs) = '' ]] return 0
if [[ $#BUFFER -eq 0 ]] {
  fg
  # BUFFER="fg"
  # zle accept-line
} else {
  # zle push-input
  fg
  # zle clear-screen
  #
  # BUFFER="fg"
  # zle accept-line
  #
  # BUFFER=""
  # zle get-line
}
zle redraw-prompt
# vim:fenc=utf-8 ft=zsh
