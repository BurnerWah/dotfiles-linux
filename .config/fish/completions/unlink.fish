# FIXME this should only complete 1 file instead of completing them endlessly (I think)
complete -c unlink -r
complete -c unlink -l help -d "Display help & exit"
complete -c unlink -l version -d "Display version & exit"
