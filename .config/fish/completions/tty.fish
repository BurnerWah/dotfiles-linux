complete -c tty -f
complete -c tty -s s -l silent -l quiet -d "Print nothing, only return an exit status"
complete -c tty -l help -d "Display help & exit"
complete -c tty -l version -d "Display version & exit"
