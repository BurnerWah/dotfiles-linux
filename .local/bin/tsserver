#!/usr/bin/env node
/*
 * The nodejs-typescript package for fedora (at time of writing) doesn't give
 * the tsserver entry point execute permissions, and doesn't link it into
 * /usr/bin. This script is just an alternate entry point until that's fixed.
 */
require('/usr/lib/node_modules/typescript/lib/tsserver');
